
      {{#each posts as |post| }}
      <section class="blog-post">
        <div class="blog-title">
         <a href="/dashboard">{{title}}</a> 
        <p>Posted by: {{username}} on {{format_date date_created}}</p>
        </div>
        <div class="blog-content">
         <p>{{content}}</p>
        </div>
         </section>
 
    {{/each}}

 
